{
  "api_key": "<%= apiKey %>",
  "data": {
    "item" : [
      <% for (var i = 0, j = stats.length; i < j; i++) { %>
        <% if (i > 0) { %>,<% } %><%= stats[i].responsiveness %>
      <% } %>
    ],
    "settings" : {
      "axisx" : [
        <% for (var i = 0, j = stats.length; i < j; i++) { %>
          <% if (i > 0) { %>,<% } %>"<%= moment(stats[i].timestamp).format('hA') %>"
        <% } %>
      ],
      "axisy" : [],
      "color" : "#ff00aa"
    }
  }
}