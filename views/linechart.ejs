{
  "api_key": "<%= apiKey %>",
  "data": {
    "item" : [
      <% for (var i = numTick; i--; ) { %>
        <%= stats[i].time %><% if (i > 0) { %>,<% } %>
      <% } %>
    ],
    "settings" : {
      "axisx" : [
        <% for (var i = numTick; i--; ) { %>
          <% if (i == numTick || i == 0 || i % 10 == 0) { %>
            "<%= moment(stats[i].timestamp).format('h:mA') %>"<% if (i > 0) { %>,<% } %>
          <% } %>
        <% } %>
      ],
      "axisy" : [],
      "color" : "#ff00aa"
    }
  }
}